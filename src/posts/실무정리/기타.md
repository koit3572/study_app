# 기타
---
## 채수구·송수구·방수구
### 채수구
- `소화수조 저수조`에 설치하는 것으로 `소방차`에 `물`을 `공급`해 주기 위한 연결구
- `소방차`가 `2m이내`까지 `접근`할 수 있는 곳에 설치
### 송수구
- `소화설비`에 `소화용수`를 `공급`하기 위하여 `건물 외벽` 또는 `구조물 외벽`에 설치하는 관
### 방수구
- `소화설비`로부터 `소화용수`를 `방수`하기 위하여 `건물내벽` 또는 `구조물 외벽`에 설치하는 관
---